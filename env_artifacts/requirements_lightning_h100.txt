# Minimal pinned set that reproduces the working corp_gym training stack on
# Lightning AI H100 (driver 570.x, CUDA 12.8 runtime, Python 3.12).
#
# Install order matters - use setup_lightning_h100.sh, or run these groups
# sequentially with the correct index URL for each.
#
# 1) CUDA 12.8 torch wheels
#    pip install --index-url https://download.pytorch.org/whl/cu128 \
#        torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1

# 2) Matched xformers (must be force-reinstalled --no-deps to keep torch 2.7.1)
#    pip install --force-reinstall --no-deps xformers==0.0.31.post1

# 3) FlashAttention 2.8.0.post2 prebuilt wheel (torch 2.7 + cu12 + cp312)
#    pip install --no-build-isolation \
#      "https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.0.post2/flash_attn-2.8.0.post2+cu12torch2.7cxx11abiFALSE-cp312-cp312-linux_x86_64.whl"

# 4) Training libraries
accelerate==1.13.0
bitsandbytes==0.49.2
datasets==4.3.0
hf_transfer
huggingface_hub>=0.27
peft==0.19.1
torchao==0.17.0
transformers==5.5.0
trl==0.24.0
unsloth==2026.4.8
unsloth_zoo>=2026.4.9

# 5) Plotting / analysis
matplotlib>=3.10
numpy<3
pandas>=2
